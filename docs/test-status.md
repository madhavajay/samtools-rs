# samtools test.pl Status

> **Authoritative (2026-05-18, `main` `81b4d87`, bgzip+tabix honest):**
> full `perl test/test.pl` = **998 total / 966 passed / 0 failed / 32
> expected failure / 0 unexpected pass**. `test.pl` emits no per-group
> breakdown, so the individual "(NNN/NNN)" counts in the table below are
> historical/illustrative — the whole-suite figure is the verified
> signal. The parity subset runners now hard-fail without bgzip/tabix
> (preflight guard), so this cannot silently regress to a false green.

This tracks the upstream `repos/samtools/test/test.pl` groups against the Rust
`samtools-rs-cli` binary. CI now fails on both the stable subset helper and
the full upstream harness. `test_bgzip` is supplied by the external htslib
`bgzip` tool (`tabix` package in CI), matching the upstream harness contract
rather than a `samtools-rs` subcommand.

Status values:

- `passing`: byte-for-byte parity has been verified through the upstream Perl
  harness, or the listed subset is known to pass and the remaining cases are
  separately marked.
- `partial`: the command exists and has Rust integration coverage, but the
  upstream harness group is not fully passing.
- `not-yet-ported`: the command is still a stub or depends on unimplemented
  infrastructure.
- `blocked`: the command depends on missing `htslib-rs` support.
- `external`: the harness group exercises a non-samtools htslib tool that is
  provided as an environment prerequisite.

## Current Gate

- Rust gate: `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`,
  and `cargo test --workspace` are expected to pass.
- Required parity subset: `.github/workflows/ci.yml` runs
  `scripts/run-passing-parity-subset.py`, which generates a temporary filtered
  copy of upstream `test.pl`. This is a stable CI subset rather than every
  row marked `passing`; the enforced groups are currently
  `test_reference`, `test_dict`, `test_faidx`, `test_fqidx`, `test_index`,
  `test_mpileup`, `test_view`, `test_import`, `test_bam2fq`, `test_sort`, `test_merge`, `test_collate`, `test_calmd`, `test_fixmate`,
  `test_idxstat`, `test_quickcheck`, `test_head`, `test_cat`,
  `test_reheader`, `test_addrprg`,
  `test_markdup`, `test_bedcov`, `test_split`, `test_large_positions`, `test_coverage`, `test_stats`, `test_checksum`, `test_depad`, `test_reset`,
  `test_ampliconclip`, and `test_ampliconstats`.
- Required regression subset: CI also runs
  `scripts/run-passing-regression-subset.py`, which executes stable upstream
  `regression.sh` files. The enforced files are currently
  `test/consensus/consensus.reg` and `test/cram_size/cram_size.reg`;
  direct C-vs-Rust smoke also locks `cram-size`, `cram-size -v`, and
  `cram-size -e` text output.
- Full parity gate: CI stages the Rust binary at the ignored
  `repos/samtools/samtools` path and runs `cd repos/samtools && perl test/test.pl` as a
  required gate.

## Harness Groups

| `test.pl` group | Status | Evidence / next work |
| --- | --- | --- |
| `test_reference` | passing | **Entire upstream `test_reference` byte-exact.** SAM/BAM/CRAM MD-tag reconstruction; CRAM MD path with `-T` or `@SQ UR:` reference discovery; embed_ref CRAM read+write in the vendored noodles fork + `view -O cram,embed_ref=1`; `-e`/`--embedded` faithful `cram2ref` extraction. Building the embed_ref CRAM via `view -e EXPR -O cram,embed_ref=1 -T ref`, all four invocations — `reference` (MD, no `-T`), `reference -e`, and both `-r 17:1000-1500` variants — match `reference/mpileup.{MD,embed}.fa{,.reg}.expected` (tests `reference_embed_ref_full_test_reference_byte_exact`, `reference_cram_md_path_with_reference_matches_upstream`). Direct C-vs-Rust smoke also locks MD full/region output, MD `-o FILE`, and `reference -e` over a C-generated embedded-reference CRAM, including coverage stderr lines and region headers without a `length:` suffix. |
| `test_bgzip` | external | `bgzip` is an htslib tool, not a `samtools-rs` subcommand. CI installs it from the `tabix` package, and the full upstream harness passes with `bgzip` on `PATH`. |
| `test_faidx` | passing | The full upstream `test_faidx` group passes (8/8): FASTA index creation plus uncompressed and gzip-compressed region retrieval for the checked local ranges. Rust coverage also verifies local `-@` and top-level `--threads` route BGZF retrieval output through worker-count BGZF paths, and the direct C-vs-Rust smoke locks `.fai` index bytes, the default region header form without a `length:` suffix, plus zero-length/truncated/`--continue` missing-region warning output. Non-fixture follow-up: broader BGZI edge cases, compressed output/indexing options, and compression-level effects. |
| `test_fqidx` | passing | The full upstream `test_fqidx` group passes (16/16): FASTQ index creation plus uncompressed and gzip-compressed region retrieval for the checked local ranges. Rust coverage also verifies local `-@` and top-level `--threads` route BGZF retrieval output through worker-count BGZF paths, and the direct C-vs-Rust smoke locks `.fai` index bytes, the default region header form without a `length:` suffix, plus zero-length/truncated/`--continue` missing-region warning output. Non-fixture follow-up: broader BGZI edge cases, compressed output/indexing options, and compression-level effects. |
| `test_dict` | passing | The full upstream `test_dict` group passes (5/5): local FASTA, bgzip-compressed FASTA, stdin FASTA, alias header output, and alt-location header output. Covered by `crates/samtools-rs/tests/dict.rs`. Direct C-vs-Rust smoke locks both stdout and `-o FILE` output. |
| `test_index` | passing | The full upstream `test_index` group passes (26/26), including threaded duplicates: large-reference CSI indexing/query, default BAI output, explicit `-o` and legacy `<in> <out.idx>` index destinations, `-M` multi-file indexing, `view -X` custom-index queries, `merge -X -R` with one custom index per BAM input, and `view --write-index` auto-indexing for BAM/CRAM/BGZF-SAM outputs. Direct C-vs-Rust smoke now locks explicit `index -o` BAI bytes for a stable BAM fixture. Rust coverage also verifies local `--threads=2` and the top-level global thread setting route BAM BAI construction through the worker-count index path while preserving deterministic BAI bytes. Non-fixture follow-up: CRAI thread parity and broader throughput comparison against C samtools. |
| `test_mpileup` | passing | The full upstream `test_mpileup` group passes (7/7): multi-input `-b` BAM/CRAM lists, `file://` input-list paths, default BAQ-adjusted qualities against BGZF FASTA references, `-B --ff 0x14`, CRAM decoding against bgzipped FASTA via the htslib-rs reference repository, and the overlap-removal fixture. The upstream `test/mpileup:depth.reg` mpileup-simulation lines also pass. Direct C-vs-Rust smoke now locks successful BAQ multi-BAM-list output, `-B --ff 0x14` stdout and `-o FILE`, overlap-removal output, stderr sample-count lines, and the missing-input error path. Rust coverage includes `mpileup_default_baq_matches_upstream_out1`, `mpileup_minus_b_ff_matches_upstream_out3`, `mpileup_overlap_removal_matches_upstream_out5`, and htslib-rs BAQ/BGZF-reference tests. Non-fixture follow-up: `@RG`-`SM` sample grouping, VCF/BCF (`-g`/`-v`), and base-modification/extra output columns. |
| `test_usage` | passing | The full upstream `test_usage` group passes (40/40) when run under a TTY. The dispatcher now prints a stdout `Usage: samtools <subcommand> [options]` banner for no-argument subcommands invoked from a terminal, matching the harness contract and avoiding terminal reads/hangs. `tview` is registered and dispatchable. Not added to the default non-TTY CI subset because upstream `test.pl` skips usage tests when no TTY/PTY is available. |
| `test_view` | passing | The full upstream `test_view` group passes (445 total: 427 passed, 18 expected failures). Covered surface includes SAM/BAM/reference-backed CRAM basics, stdin paths, count/header, filters, BED and positional region queries (including `chr:pos` open-ended regions, two-column BED point semantics, and `-L` intersection with positional regions), expression filtering, `-z`/`--sanitize`, `--save-counts FILE`, count-only no-reference CRAM `--save-counts` for simple summary-backed filters, reference-independent MAPQ/flag expressions, and read-group/library/aux-tag filters, `-m INT`, `-N`/`--qname-file FILE`, read-group/library filters, aux-tag filters, `-U`/`-p` split/unmap output for SAM/BAM/CRAM input to SAM/BAM/reference-backed CRAM output, `-x`/`--keep-tag` aux stripping for SAM/BAM/CRAM input to SAM/BAM/reference-backed CRAM output, `-t FILE` / `-T FILE` missing-`@SQ` dictionary injection, `-B`, `-s`, `--remove-flags`, `--exclude-flags`, `--fetch-pairs`, CRAM stdin SAM float spelling, and SAM/BAM/CRAM output paths used by the harness. Direct C-vs-Rust smoke locks SAM text output, BAM `-h` text output, count-only output, MAPQ/required-flag/filtering-flag count filters, indexed BAM region text output, MAPQ, `flag.proper_pair`, CIGAR-derived (`endpos`/`rlen`/`sclen`/`hclen`), and mate/reference (`mpos`/`pnext`/`tlen`/`rnext`/`mrname`/`refid`/`mrefid`/`ncigar`) expression filters, qname allow/deny filters, read-group filters including upstream's no-`RG` pass-through unless `-n`, library filters, aux-tag value/file filters, rendered BAM/CRAM binary aux-tag rewrite output, and `view -c --save-counts` JSON sidecar output, including no-reference CRAM MAPQ/flag expression and read-group/library/aux-tag filter counts. Non-fixture follow-up: no-reference CRAM save-counts for text/binary output and reference-dependent expression counts, multi-file inputs, paired filters, and deeper CRAM performance/streaming parity. |
| `test_head` | passing | The full upstream `test_head` group passes (10/10): default output, `-h` 0/1/5/29/30, `-n` 0/1/5, and stdin with `-h 5 -n 5`. Rust coverage remains in `crates/samtools-rs/tests/head.rs` plus `commands::head` unit tests. |
| `test_cat` | passing | The full upstream `test_cat` group passes (26/26): BAM, recompressed BAM, CRAM visible concatenation, CRAM region paths, stdout redirection, `-p 1/2` + `-p 2/2`, and `-h` replacement headers. Direct C-vs-Rust smoke now locks upstream-shaped SAM-input rejection. Non-fixture follow-up: true CRAM-preserving concatenation and BGZF block-level BAM fast path; the current CRAM fixture path writes a SAM-visible stream. |
| `test_import` | passing | The full upstream `test_import` group passes (21/21): single `-0` and interleaved `-s` FASTQ roundtrips through `fastq`, paired positional and `-1`/`-2` imports, CASAVA `-i` and alternate barcode tags, selected/all FASTQ definition aux tags, `-R`/`-r` read-group forms, positional interleaved FASTQ detection, explicit `--i1`/`--i2` index reads with barcode/quality tags, and UMI extraction. Direct C-vs-Rust smoke now locks paired read-group SAM output and interleaved `-T ""` aux-output SAM bytes. Rust regression `import_fastq_to_cram` covers CRAM output for the unmapped import stream, including `.cram` extension inference and `--output-fmt=cram`. |
| `test_bam2fq` | passing | The full upstream `test_bam2fq` group passes (84/84), including threaded duplicates. SAM/BAM/CRAM FASTQ/FASTA output covers `-f`/`-F`/`-G`, `-0`, `-n`/`-N`, `-O`, `-v`, `-U`/`--UMI-tag`, `-i`/`--barcode-tag`, selected/all aux comments with accumulating `-t`/`-T` (including compact `-Ts0` spelling), repeated `-d`/`-D` value-union filters, upstream-style name-grouped split outputs, paired-path interleaving, `--i1`/`--i2` with `--index-format`/`--quality-tag`, headerless `.sam` index extraction, reverse-strand reverse-complementing on the default single-output paths, and `--no-sc` / `--no-sc-bkp` / `--sc-aux` soft-clip trimming + backup aux fields. CRAM input uses explicit top-level `--reference` or discoverable references; the generated `view -C` setup roundtrip is covered by vendored noodles CRAM mate/MAPQ preservation fixes. Direct C-vs-Rust smoke locks default FASTQ stdout plus the `[M::bam2fq_mainloop]` informational stderr summary and the `-0 FILE` routing shape where paired reads still go to suffixed stdout. Rust regressions: `fastq_cram_input_uses_top_level_reference`, `view_cram_roundtrip_preserves_bam2fq_setup_fields`. |
| `test_depad` | passing | The full upstream `test_depad` group passes (9/9): SAM and BAM input, default BAM output, `-s` SAM output, and `-u`/`-1` BAM output modes against the padded `depad.001` fixture with `-T` and `--no-PG`. The implementation reuses the locked SAM depadding transform for SAM/BAM/reference-backed CRAM input, converts the depadded stream to BAM for binary output, and derives an unpadded FASTA for CRAM output (`-O cram`, `--output-fmt=cram`, or `.cram`). Direct C-vs-Rust smoke now locks `depad -T ... -s --no-PG` SAM text output, including upstream's preserved `@SQ M5` field while `LN` is rewritten. Rust regression `depad_cram_input_and_output_roundtrip` covers CRAM input and output. |
| `test_stats` | passing | The full upstream `test_stats` group passes (42 total: 38 passed, 4 expected failures): SN, FFQ/LFQ, GCF/GCL, target and positional regions, overlap handling, barcode histograms, read-group/sample filtering, big-deletion and ref-stats RFS fixtures. The final blocker was the `--ref-stats -t targets ref1` harness variant; RFS reporting now prefers the target file's intervals while positional regions still drive the no-`-t` case. READ_OTHER records now match upstream for average-quality accumulation, the properly-paired percentage denominator, no-reference GCD zero-GC contribution behavior, and reference-backed MPC/GCD handling of ambiguous reference bases. Tests `stats_matches_upstream_stat_fixtures`, `stats_no_reference_gcd_matches_upstream_multibin_shape`, and `stats_filters_required_and_filtering_flags` cover the locked fixture set plus direct multi-bin GCD and flag-filter average-length behavior. Direct C-vs-Rust smoke now includes raw default `stats dat/test_input_1_a.bam` output plus representative `-d`, `-f`, `-F`, `-i`, `-m`, `-l`, `-c`, `-g`, `-t`, `-q`, reference-backed, `--ref-stats`, reference-backed `--ref-stats`, and positional-region paths, including the upstream-compatible produced-by and command-line header lines, without harness normalization. Non-fixture follow-up: broader CRAM no-region per-cycle/quality/COV parity and remaining pileup COV/GCD edge cases. |
| `test_depth` | passing | The upstream `test/mpileup:depth.reg` fixture passes (55 expected passes, 1 expected failure): multi-input depth, `-r`, `-b`, `-a`/`-aa`, BED/reference ordering, deletion-touch and `-J` deletion-depth behavior, `-s` paired-overlap removal, flag filters, and the companion `mpileup -ABQ0` depth-simulation commands. Direct C-vs-Rust smoke locks both stdout and `-o FILE` output for a stable region. CRAM region depth works with an explicit reference or the no-reference synthetic-query path. Remaining non-fixture follow-up: broader pileup-edge parity. |
| `test_merge` | passing | The full upstream `test_merge` group passes (28/28), including threaded duplicates: seeded `@RG`/`@PG` reconciliation, `-b FILE` input-list ordering before trailing positional inputs, `-r`, `-c`/`-p`, stdout `-`, PG-tag merge ordering for coordinate and `-n` natural-name secondary sort, `--template-coordinate`, `--no-PG`, and `-L BED` regions named by `@SQ AN` aliases. Direct C-vs-Rust smoke now locks `--no-PG` SAM output for seeded three-input merge, `-r` filename-derived read groups, and template-coordinate merge. Tests `merge_reconciles_rg_pg_byte_exact_vs_upstream`, `merge_input_list_precedes_remaining_positionals_like_upstream`, `merge_tag_sort_ties_match_upstream_secondary_order`, `merge_template_coordinate_matches_upstream_fixture`, `merge_l_bed_resolves_reference_aliases`, and `merge_writes_cram_output_with_top_level_reference`. Streaming/k-way merge remains a non-fixture follow-up. |
| `test_sort` | passing | **Every upstream `test_sort` fixture is byte-exact (modulo the harness' `@PG`/`VN` strip)**: coordinate (`pos`), `-n` natural name (`name`,`name3`), `-N` lexicographical name (`name2`), `-t` aux-tag (`tag.rg`,`tag.rg.n`,`tag.as`,`tag.fi`), `-M` minimiser non-indexed/`-I` indexed/`-MH` squash (`minimiser-{basic,indexed,indexed-poly}`, incl. the `reset --dupflag` fresh-header rebuild), and `--template-coordinate` (full `template_coordinate_key`/`bam1_cmp_template_coordinate`/`unclipped_*`/`lookup_libraries` port, `@HD GO:query`). Tests `sort_matches_upstream_test_sort_fixtures` + `sort_minimiser_all_variants_match_upstream`; `sort_cram_input_uses_top_level_reference` covers reference-backed CRAM input and CRAM output with top-level `--reference`. Remaining (not fixture-blocking): external/temp-file merge for very large inputs and memory/thread caps. |
| `test_collate` | passing | Byte-exact vs the ENTIRE upstream test_collate harness (6/6): tolerant SAM reader, the exact bamshuf order (bucket by hash_X31_Wang(qname)%64, sort by hash,qname,flag>>6&3), raw-header SAM with @HD SO:unsorted GO:query, -o-extension format inference including `.cram`, and fast-mode ring eviction (evict-after-insert deferral). Tests collate_* + collate_matches_upstream_test_collate_fixtures in sort_merge.rs; `collate_cram_input_uses_top_level_reference` covers reference-backed CRAM input and CRAM output with top-level `--reference`. On-disk hash-bucket for >memory inputs remains. |
| `test_fixmate` | passing | The full upstream `test_fixmate` group passes (42 total: 40 passed, 2 expected failures), including threaded duplicates: coordinate-sort rejection, TLEN, default MC/MQ tags, `-c` template-CIGAR `ct` tags, sanitizer fixture parity, and all base-modification `-M` fixtures (`MM`/`ML`/`MN`, draft `Mm`/`Ml`, hard-clip trimming, invalid-tag deletion, no-sequence cases). Test `fixmate_matches_upstream_group` covers the fixture set; Rust regression `fixmate_accepts_reference_backed_cram_input` covers reference-backed CRAM input and CRAM output with top-level `--reference` (`-O cram` / `--output-fmt=cram`). |
| `test_calmd` | passing | The upstream `test_calmd` invocation (`calmd -uAr mpileup.1.sam mpileup.ref.fa` → BGZF) passes: getopt-style `-uAr` cluster split, `-A` applies recalculated BAQ to QUAL, `-b`/`-u` emit BGZF BAM output; SAM-input MD/NM recompute and BAQ paths with `@PG`/`--no-PG`. Direct C-vs-Rust smoke now locks default `calmd --no-PG mpileup.1.sam mpileup.ref.fa` SAM stdout/stderr/exit parity, including unchanged `MD`/`NM` aux order and unmapped-record handling, plus `-e` matching-base conversion, `-r -e` BAQ plus matching-base conversion, `-d` dropping every aux tag except `RG`, `-n` masking diagnostics, `-Q` quiet suppression, `-C` MAPQ capping, `-q` quality binning, and `-N` no-MD/NM-update behavior. Test `calmd_dash_u_a_r_emits_bgzf_bam_like_upstream` covers BAM output, `calmd_writes_cram_output_with_reference` covers `.cram` inference plus `--output-fmt=cram` CRAM output from the recalculated SAM stream, `calmd_cap_mapping_quality_uses_sam_cap_mapq` covers `-C cap`, `calmd_max_nm_masks_matching_bases_and_qualities` covers `-n max_nm` masking, `calmd_dash_e_changes_matching_bases_for_mapped_and_unmapped_records` covers `-e`, `calmd_dash_q_bins_base_qualities` covers `-q`, `calmd_dash_cap_n_preserves_existing_md_nm_tags` covers `-N`, and `calmd_baq_accepts_bam_and_cram_input` covers BAQ over BAM/CRAM input via the SAM bridge. Remaining (not fixture-blocking): broader upstream MD/BAQ option byte parity. |
| `test_idxstat` | passing | Byte-exact (stdout + stderr) vs `idxstats/test_input_1_a.bam.expected` for `test_input_1_a.bam`, `.sam`, and `.cram`. CRAM without an explicit reference is supported via the htslib-rs synthesizing-reference summary path. |
| `test_quickcheck` | passing | TODO marks byte-for-byte parity against `quickcheck/all.expected`; exit-code coverage lives in `crates/samtools-rs/tests/quickcheck.rs`, and CLI stdout/stderr parity lives in `crates/samtools-rs-cli/tests/quickcheck.rs`. |
| `test_reheader` | passing | The full upstream `test_reheader` group passes (7/7): BAM header replacement, CRAM v2.1/v3.0 visible reheader paths, in-place harness paths, and `-c <command>` header filtering. Direct C-vs-Rust smoke now locks upstream-shaped SAM-input rejection. Non-fixture follow-up: true CRAM-preserving in-place/binary rewrite and BAM BGZF fast path; the current CRAM harness path rewrites the SAM-visible stream. |
| `test_addrprg` | passing | The full upstream `test_addrprg` group passes (14/14), including threaded duplicates of every case: `overwrite_all`, `orphan_only`, unknown `-R` rejection stderr, full `@RG` `-r`, `-R` existing ID reuse, incremental `-r ID`/`-r CN`, and `-w` header edit. SAM output routes through `sam_render` for htslib `%g` float aux spelling. Reference-backed CRAM input is covered by `addreplacerg_accepts_reference_backed_cram_input`; `-O cram`/`--output-fmt[=]cram` with `-T`/`--reference` writes reference-backed CRAM via temp-BAM → shared CRAM writer. Non-fixture follow-up: mate-aware behavior. |
| `test_markdup` | passing | **Byte-exact vs the entire upstream `test_markdup` SAM harness — all 14 passing fixtures `markdup/{5..18}` plus upstream-shaped `1..4` expect-fail exits/partial output**. Faithful `bam_markdup.c` port: `make_pair_key` (template + `--mode s` sequence, unclipped coords via CIGAR/`MC`, `R_LE`/`R_RI` mate discriminator), `make_single_key`, `calc_score`+`ms` with QCFAIL/qname tie-break, `-S` `dup_hash` propagation, `get_coordinates_colons` + regex `get_coordinates`, the full `find_duplicate_chains` optical-chain re-tagging, `--use-read-groups`, `--duplicate-count`, `--read-coords`/`--coords-order`/`--barcode-rgx`/`--barcode-name`, raw-header SAM output, reference-backed CRAM input/output through the temp SAM/BAM bridges, and `-s` stats counts matching the promoted fixture matrix (`5..18`). Tests `markdup_matches_upstream_test_markdup_fixtures`, `markdup_upstream_expect_fail_cases_return_one_with_expected_partial_output`, `markdup_stats_match_upstream_fixture_counts`, and `markdup_accepts_reference_backed_cram_input_and_output`. |
| `test_bedcov` | passing | The full upstream `test_bedcov` group passes (8/8): BAM coverage, `-j`, attached `-g512 -G2048`, `-c`, and all `-H` header cases including custom headers, empty source header fields, and BED12-derived placeholder columns. Direct C-vs-Rust smoke locks default output, `-j`, `-g512 -G2048`, and `-c -H` text output. CRAM bedcov works with an explicit reference or the no-reference synthetic-query path for the current CIGAR-only metric. |
| `test_split` | passing | The full upstream `test_split` group passes (18/18), including threaded duplicates of every case: `%#`/`%!` template expansion, extension-based output-format inference, padded output indexes, `-d RG`, string/integer aux-tag splitting, `-M` overflow routing, sorted-by-tag stdin pipelines (`sort -t nn | split -`), unaccounted output, and harness header reordering / `@PG` normalization. Direct C-vs-Rust smoke now locks `.sam` template inference on a stable RG-only BAM plus the missing-RG/no-`-u` error path. SAM output routes through `sam_render::write_record`; whole-file CRAM input is supported through the htslib-rs all-record iterator for split-to-SAM/BAM workflows used by `test_checksum`, and `split_sam_input_by_rg_to_cram_outputs_with_reference` covers CRAM output with top-level `--reference`. Non-fixture follow-up: sorted-by-tag streaming beyond the in-memory harness path. |
| `test_large_positions` | passing | The full upstream `test_large_positions` group passes (9/9): BGZF-SAM readback, CSI indexing setup, region and `-L BED` queries over large SAM coordinates, coordinate sort tie ordering, merge, depth, depth BED, noninteractive `tview -d T -p`, and the `sort -n | fixmate -O sam - - | sort` large-TLEN pipeline. Direct C-vs-Rust smoke now locks the noninteractive `tview` text output over an indexed BGZF-SAM setup. Non-fixture follow-up: broader interactive/curses/HTML `tview`, true indexed SAM seeking instead of the current sequential SAM-region fallback, and binary formats for >i32 TLEN remain constrained by the underlying record model. |
| `test_ampliconclip` | passing | Full `bam_ampliconclip.c` port (from a stub). **Byte-exact vs the entire upstream `test_ampliconclip` harness** (10 SAM fixtures + 3 primer-count TSVs), plus the dormant current-upstream `3_multi_ref_both_clip` `--both-ends` multi-reference edge: per-ref BED, `matching_clip_site`, soft/hard `bam_trim_left`/`right`, `--both-ends`, `--original` `OA`, `--keep-tag`/`NM`-`MD` drop, `--filter-len`/`--fail-len`/`--unmap-len`, `--strand`, `--primer-counts`, raw-header `@HD SO:coordinate→unknown`. Tests `ampliconclip_matches_upstream_test_ampliconclip_fixtures` and `ampliconclip_accepts_reference_backed_cram_input_and_output` cover SAM fixture parity plus reference-backed CRAM input/output. BGZF fast path remains. |
| `test_ampliconstats` | passing | Full `amplicon_stats.c` port (~1776 lines, from a stub). **Byte-exact (modulo harness-stripped version/command-line) vs the entire upstream `test_ampliconstats` harness** (`stats`, `stats_mixed`, `stats_partial`): `count_amplicon`/`bed2amplicon`, ±`pos-margin` lookup, `accumulate_stats` (qname overlap removal, depth, amplicon classification, tcoord freq), `append_lstats`, the full multi-section `dump_stats` with `COMBINED` MEAN/STDDEV. `-s`/`--use-sample-name` uses the first `@RG SM:` value with filename fallback, `--tcoord-bin` aggregates nearby template-coordinate observations using upstream's frequency-first clustering rule, and SAM/BAM/reference-backed CRAM input is covered. Direct C-vs-Rust smoke now locks all three output-file shapes with metadata lines stripped. Tests: `ampliconstats_matches_upstream_test_ampliconstats_fixtures`, `ampliconstats_use_sample_name_uses_first_read_group_sample`, `ampliconstats_accepts_bam_and_reference_backed_cram_input`, `aggregate_tcoord_merges_nearby_same_status_into_most_frequent_site`. |
| `test_reset` | passing | The full upstream `test_reset` group passes (18/18): basic.1.mp.1 (reset\|view, stdin, file), basic.output.mp.1 (-o SAM from stdin), basic.bam.input (-o SAM from BAM), basic.cram.input (-o SAM from CRAM with adjacent FASTA discovery when no explicit reference is supplied), output.nRG.* (`--no-RG` plus keep-tag precedence), output.keep.* (`--keep-tag`, `--remove-tag`, `--remove-tag ^...` unioned with `--keep-tag`), output.flg.* (flag update + reverse flip), and reject.1/reject.2 (the positional `--reject-PG` "onwards" removal). `reset_matches_upstream_test_reset_fixtures` locks the fixture set; Rust regression `reset_writes_cram_output` covers `-O cram`, `--output-fmt=cram`, and `.cram` output paths by encoding the now-unmapped reset stream against an empty temporary FASTA. Non-fixture follow-up: broader CRAM reference-discovery parity. |
| `test_checksum` | passing | The full upstream `test_checksum` group passes (14/14), including threaded duplicates: default BAM checksums, embedded-reference CRAM checksums without explicit `--reference`, `-a`, `-qv`, and split-per-read-group checksum merging for BAM and CRAM inputs. Direct C-vs-Rust smoke locks both stdout and `-o FILE` checksum reports. `checksum` uses the htslib-rs all-record CRAM iterator with explicit-reference and no-reference paths; `split` now accepts CRAM input for the harness split/merge flow. Rust coverage includes `checksum_cram_matches_bam_via_all_record_iterator` and `split_cram_input_by_rg_to_sam_outputs`. Non-fixture follow-up: broader reference-compressed CRAM checksum coverage. |
| `test_coverage` | passing | The full upstream `test_coverage` group passes (6/6): default sample coverage, `--min-depth`, `-Q`/`-q`, and multi-input aggregate rows. CRAM region coverage works with an explicit reference or the no-reference synthetic-query path for the current CIGAR/quality metrics. Direct C-vs-Rust smoke now locks default BAM coverage over `test_input_1_a.bam`, including HTSlib's `0xff` missing-quality sentinel in `meanbaseq`, plus `-o FILE`, fixed-width ASCII `-m` histogram, `-D` depth-plot output, an uneven-bin-tail histogram, and `$COLUMNS`-driven default-bin selection. Histogram/depth-plot coverage now includes UTF-8/ASCII glyphs, sidebars, x-axis labels, upstream x-axis tick placement, and true `-D` mean-depth bins (`coverage_depth_plot_uses_depth_bins_not_breadth_histogram`). Non-fixture follow-up: broader byte-parity for interactive TTY-width histogram edge cases. |

## Rust Integration Coverage

Current Rust integration tests live under `crates/samtools-rs/tests/`:

- `dict.rs`
- `exit_codes.rs`
- `flags.rs`
- `head.rs`
- `misc.rs`
- `phase.rs`
- `quickcheck.rs`
- `sort_merge.rs`
- `stats_wave_d.rs`
- `targetcut.rs`
- `tview.rs`
- `view.rs`

These are useful development checks, but they do not replace the upstream
`test.pl` parity rows above.

The required CI subset is maintained in
`scripts/run-passing-parity-subset.py` and
`scripts/run-passing-regression-subset.py`; update their default lists whenever
a row or regression file is stable enough to gate.
